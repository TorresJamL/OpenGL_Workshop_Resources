{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch GLENGINE",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "externalConsole": true,
      "MIMode": "gdb"
    }
  ]
}